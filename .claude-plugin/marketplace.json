{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "claude-ship",
  "description": "Distributes the ship workflow that turns a request into an Issue and implements it until green, together with its dependent skills (create-issue / issue-loop / code-review).",
  "owner": {
    "name": "Seiya Kawamura"
  },
  "plugins": [
    {
      "name": "ship-session",
      "source": "./plugins/ship-session",
      "description": "A workflow that turns a request into a GitHub Issue and implements it in the same session until every acceptance criterion is met, verification is green, and the review has zero findings. It decides the goal (Issue / implementation / PR / merge) once before starting and stops there."
    },
    {
      "name": "graph-workflow",
      "source": "./plugins/graph-workflow",
      "description": "Breaks a task into a graph of nodes and edges and runs it deterministically in parallel with the Workflow tool. It shows the graph design for approval first and resumes midway on failure."
    }
  ]
}
